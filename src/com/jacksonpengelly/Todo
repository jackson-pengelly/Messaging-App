Add an admin account system for server owners
Create a server join system
Add the prompting and logic for someone running Server.Main to create a server
Add a system where server owners can make join codes for their servers then it will fetch the IP and Port specified from the Postgresql database and connect the user to that server (also could feature a password system)

Eventually fork this over into a different project with one general chat room and let users create accounts and private message each other.